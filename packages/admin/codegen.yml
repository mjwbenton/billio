overwrite: true
schema: "https://api-readonly.billio.mattb.tech"
documents: "src/**/*.{ts,tsx}"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      avoidOptionals: true
      preResolverTypes: true
      skipTypename: true
